name: compilation
on:
  push:
    branches:
      - '*'
      - '!*WIP'
jobs:
  macos-compilation:
    runs-on: macos-10.15
    steps:
      - uses: actions/checkout@v2
      - run: make -C philo/
  linux-compilation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: make -C philo/
